[build-system]
requires=["setuptools"]
build-backend = "setuptools.backends.legacy:BuildBackend"

[project]
name="gitcheck"
version="0.1.0"
dependencies = [
    "gitpython","type-sitter", "pyyaml", "click", "rich",
]

[project.scripts]
gitcheck="cli.main:run_pipeline"