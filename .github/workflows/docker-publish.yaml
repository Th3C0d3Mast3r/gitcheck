# NOTE: DO NOT CHANGE/EDIT THIS
# have made the changes to the rules in the settings as well- so yes. Its COLLABORATOR level access there, and NOT CONTRIBUTOR
name: Build & Push Docker Image

on:
  push:
    tags:
      - 'v*'   # triggers on version tags like v1.0.0

jobs:
  docker:
    if: github.actor=='Th3C0d3Mast3r' || github.actor=='Piyush123abc'
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Extract version
        id: vars
        run: echo "VERSION=${GITHUB_REF#refs/tags/}" >> $GITHUB_OUTPUT

      - name: Build & Push
        uses: docker/build-push-action@v5
        with:
          context: .
          # this hopefully works
          file: ./docker/Dockerfile
          push: true
          tags: |
            dcodemaster/gitcheck:${{ steps.vars.outputs.VERSION }}
            dcodemaster/gitcheck:latest
